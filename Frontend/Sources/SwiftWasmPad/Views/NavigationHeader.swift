import TokamakDOM


struct NavigationHeader: View {
    var body: some View {
        HStack {
            Text("SwiftWasm")
                .font(.title)
                .padding()
            Spacer()
        }
    }
}
